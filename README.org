* fmt-examples
Просто небольшая "шпаргалка" по использованию fmt в C++ с разными типами данных.

** main.cpp
Демонстрирует возможности форматирования с помощью ~fmt~:

- =int= — целые числа
  - Автоматический вывод
  - Шестнадцатеричный формат (={:x}=)
  - С префиксом =0x= (={:#x}=)
  - С нулевым заполнением до 6 символов (={:#06x}=)

- =std::string= — строки
  - Автоматический вывод
  - Выравнивание по правому краю с шириной (={:>10}=)

- =double= — числа с плавающей точкой
  - Автоматический вывод
  - Ограничение точности (={:.2f}=)
  - Научная запись (={:e}=)

- =bool= — логический тип
  - Вывод как =true=/=false=
  - Тернарный оператор для текстового представления

- =char= — символы
  - Вывод символа
  - Преобразование в код символа (=static_cast<int>=)

- Множественные аргументы
  - Подстановка нескольких значений в одну строку
  - Индексированные аргументы (={2}= ={0}= ={1}=)

** Сборка
#+begin_src bash
mkdir -p build && cd build && cmake .. && make
./fmt-examples
#+end_src

** История промптов
PS: Я тут во всю использовал OpenCode =) ниже история промптов)

#+begin_quote
сделай базовый проект на C++ с cmake
#+end_quote

#+begin_quote
cпасибо!
#+end_quote

#+begin_quote
добавь стандартный заголовок GNU GPLv3 лицензии в main.cpp файл c названием проекта и автором: Kirill Pshenichnyi <pshcyrill@mail.ru>
#+end_quote

#+begin_quote
добавь в main.cpp, ниже уже существующих, примеров с использованием fmt в println c разными типами переменных
#+end_quote

#+begin_quote
добавь в README.org более развернутое описание того что в main.cpp
#+end_quote

#+begin_quote
Придумай описание проекта для github, что можно было бы написать в поле Description
#+end_quote

#+begin_quote
Добавь список предыдущих промптво в README.org, каждый добавляя в #+begin_quote
#+end_quote
